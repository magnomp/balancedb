# Task M2 — Migrations: on-the-fly schema creation + versioning

Read first: `tasks/_common.md`; `docs/plan.md` §M2 (the 6-step runner design — implement it as specified) and §0; `docs/architecture-spec.md` §5.2 (the DDL, to be embedded verbatim as `migrations/0001_init.sql` plus seed rows) ; `docs/decisions/0001-custom-migration-runner.md`.

Prereq: M1.5 merged.

Mission: `internal/migrate` runner (~150 lines: schema create, advisory lock keyed on schema name, per-schema `schema_migrations`, embedded `migrations/*.sql` applied in order, each in its own tx), wired into api/processor boot (respecting `BALANCEDB_MIGRATE_ON_START`) and the `migrate` role. Real `make itest` target now: integration-test helper creating/dropping throwaway schemas (`test_<random>`) from `TEST_DATABASE_URL` — this helper is used by every later milestone.

Done when: plan §M2 "Done when", including the parallel-migrators race test and the two-schemas-one-database independence test. Verify: `make lint && make test && make itest`.
