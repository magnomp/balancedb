# Task M1 — Skeleton: config, DB pool, role dispatch

Read first: `tasks/_common.md`; `docs/plan.md` §0 (global decisions — all of it, it governs every choice here) and §M1; `docs/architecture-spec.md` §2–§3.

Prereq: none. You are the first agent; also initialize the repo: `git init`, `go mod init` (module path per operator; default `github.com/OWNER/balancedb` — leave a TODO if unknown), `.gitignore`, `Makefile` with `lint`/`test`/`itest` targets (itest may be a stub until M2), create `docs/handoff.md` with an empty `## OPEN QUESTIONS` section. Note: `docs/decisions/0002` and `0003` already exist (operator pre-decisions); M1.5 adds the README/template around them and `0001`.

Mission: the repo layout from plan §0, `cmd/balancedb` dispatching roles `api | processor | migrate` (stubs for roles beyond boot), `internal/config` (env table from plan §0: parsing, validation, schema-name regex, DSN redaction for logs), `internal/db` (pgxpool, `AfterConnect` sets `search_path` with quoted identifier, ping-on-boot, `WithTx` helper), slog setup, SIGTERM/SIGINT context, graceful shutdown.

Done when: plan §M1 "Done when", literally. Verify: `make lint && make test`; manual: both roles boot against any reachable Postgres and log the structured startup line.
