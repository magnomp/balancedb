# Task M6 — Processor: groups + batching

Read first: `tasks/_common.md`; `docs/plan.md` §M6 and §0; `docs/architecture-spec.md` §8.3 (group processing), §8.5 (batching), §6 (per-account netting), §4.1 G2/G3.

Prereq: M5 merged.

Mission: `process_group` per spec §8.3 (net per account, all-or-nothing, skip-by-status for decided groups), then batching per §8.5 (up to batch_size decisions per DB tx, guards per decision, whole-batch rollback on any miss, idempotent reprocess).

Extend `simtest/`: groups in the reference model; the batch-crash-equals-sequential equivalence check; G2 invariant queries (no partially-applied group observable between steps); G3 exact outcome-sequence comparison.

Done when: plan §M6 "Done when" (every listed test, incl. registration-order determinism and crash/reprocess equivalence). Verify: `make lint && make test && make itest && make simtest` (add the simtest target now if absent).
