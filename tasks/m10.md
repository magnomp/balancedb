# Task M10 — Deterministic simulation testing (hardening pass)

Read first: `tasks/_common.md`; `docs/plan.md` §M10 (your full scope) and §0; `docs/architecture-spec.md` §15 (this milestone is the spec's declared first investment), §4 (G1–G6 — the assertion list).

Prereq: M6 merged (ideally M8; process faults don't need HTTP).

Mission: complete the harness that M3/M5/M6 have been growing, per plan §M10: seeded generators for the full scenario space (limits, singles, groups, aggressive back/future-dating with day crossings and timestamp ties, reversals incl. double-reversal and retry-after-reject, limit changes, idempotent retries) interleaved with fault injection (kill/restart, competing-leader failover, zombie-leader stale-lease writes, batch-boundary crashes). Reference model asserts G1/G2/G3/G4/G6 per plan §M10; every failure prints its seed; every seed reproduces.

Also: the one-time mutation smoke test (disable Guard 3 locally, confirm the harness catches it, revert, document in README).

Done when: plan §M10 "Done when" (1,000+ seeds in CI-reasonable time; mutation check documented). Verify: `make simtest` (full), `make lint && make test && make itest`.
