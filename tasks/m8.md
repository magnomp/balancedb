# Task M8 — Synchronous waiting: LISTEN/NOTIFY + poll fallback

Read first: `tasks/_common.md`; `docs/plan.md` §M8 and §0; `docs/architecture-spec.md` §10.1 "Waiting" (the contract: wait-up-to-T, never guaranteed outcome; waiting strictly optional); `docs/decisions/0002-bidirectional-listen-notify.md` (this task implements its outcome-fan-out half; M5 built the doorbell half).

Prereq: M5 + M7 merged.

Mission: `internal/notify` fan-out per plan §M8: one dedicated LISTEN `outcomes` connection per API process; subscription registry (`op:<id>`/`tx:<id>` → channels); reconnect-with-resubscribe. Wait path: register waiter → immediate status check (lost-wakeup race close) → select{notification, 1–2 s poll ticker, deadline} → 200 on outcome / 202 with current state on expiry (api_max_wait_ms cap). `wait_ms = 0` must not touch any of this machinery. Replace M7's stubbed `wait_ms > 0` behavior inside the existing Huma operation — the OpenAPI contract for the endpoint should need no shape change, only description updates (re-export `api/openapi.yaml`).

Done when: plan §M8 "Done when" — including the killed-listen-connection fallback test, the decided-before-registration test, and the end-to-end idle-system doorbell+notify wait resolving well under one loop_interval. Verify: `make lint && make test && make itest`.
