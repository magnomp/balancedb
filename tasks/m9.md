# Task M9 — Observability + operational hardening

Read first: `tasks/_common.md`; `docs/plan.md` §M9 and §0; `docs/architecture-spec.md` §13 (each metric and its rationale — the README section you'll write mirrors this table); `docs/decisions/0002-bidirectional-listen-notify.md` (two latency metrics come from it).

Prereq: M6 + M7 merged (M8 for the NOTIFY→outcome lag metric).

Mission: plan §M9: Prometheus metrics (§13 set + doorbell wakeup lag + NOTIFY→outcome lag + pgxpool stats), /healthz + /readyz (standby processor is healthy), panic-recovery middleware, request + slow-query logging, `make loadgen`.

Done when: plan §M9 "Done when": metrics visible under loadgen; README documents each metric with its §13/ADR-0002 rationale. Verify: `make lint && make test && make itest`.
