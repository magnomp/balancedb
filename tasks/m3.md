# Task M3 — Domain model + insertion core (no HTTP)

Read first: `tasks/_common.md`; `docs/plan.md` §M3 and §0; `docs/architecture-spec.md` §5 (model + conventions), §10.1 (insertion semantics + idempotency — implement the Stripe model exactly), §2 (sharding invariant — enforced here and only here); `docs/decisions/0002-bidirectional-listen-notify.md` (you emit the doorbell).

Prereq: M2 merged.

Mission: `internal/model` (row types, status constants, reason codes with machine-readable detail), amount parsing helper (json.Number → strict int64; THE single designated entry point for money), canonical payload hashing, and the insertion core as pure functions over `pgx.Tx` per plan §M3: single vs group, account upsert, atomic group insert, op_count, idempotent replay, payload-hash conflict, max_group_size (read from config table), one-owner-per-group. Every successful insert tx ends with `NOTIFY work_available` (payload-free hint; ADR-0002 — a pure wakeup, no correctness weight).

Also: start `simtest/` now (plan says the harness grows from M3): the sequential reference model's data structures + outcome computation for inserts, and a first property test that idempotent replays never duplicate (G6). Keep it small; it grows each milestone.

Done when: plan §M3 "Done when" (every listed test case; idempotent replays must not double-ring the doorbell in a way that breaks anything — it can't, but assert inserts-without-new-rows still commit cleanly). Verify: `make lint && make test && make itest`.
