# Task M4 — Leader lease

Read first: `tasks/_common.md`; `docs/plan.md` §M4 and §0; `docs/architecture-spec.md` §7.1 (the acquire/renew SQL — use it as written; DB clock only).

Prereq: M2 merged (M3 not required).

Mission: `internal/lease` per plan §M4: boot UUID, single-UPDATE acquire/renew, graceful release, `IsLeader()` refreshed by the caller's loop (no background goroutine — the loop is the heartbeat).

Constraint: no `time.Now()` in any lease comparison; TTL comes from the config table, read by the caller and passed in.

Done when: plan §M4 "Done when", including the 100-round two-instance mutual-exclusion test. Verify: `make lint && make test && make itest`.
