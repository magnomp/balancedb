# Task M11 — Docker image + prod-like compose

Read first: `tasks/_common.md`; `docs/plan.md` §M11 (Dockerfile given verbatim — use it) and §0 (Postgres 18 decision).

Prereq: M9 merged (M10 recommended before calling the image "release-worthy").

Mission: multi-stage Dockerfile from plan §M11 (static, distroless, nonroot, migrations embedded); root docker-compose.yml (postgres:18 + api + processor + optional standby processor, all via BALANCEDB_* env); README deployment section per plan §M11 (env contract, roles, multi-schema cells, upgrade procedure incl. migrate-as-gate).

Done when: plan §M11 "Done when": clean-checkout `docker compose up` → insert a group via curl, synchronous wait, read balances; kill active processor → standby takes over within lease TTL. Script this smoke test (`make smoke`) — M13 reuses it.
