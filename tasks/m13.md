# Task M13 — CI + release

Read first: `tasks/_common.md`; `docs/plan.md` §M13 and §0.

Prereq: M10 + M11 + M12 merged.

Mission: CI pipeline per plan §M13: lint → unit → itest + simtest against a Postgres 18 service container → image build → compose smoke test (reuse `make smoke` from M11) → push tagged by SHA + semver on tags. Commit golangci-lint config; enforce go vet + gofumpt. Ask the operator which CI system before assuming GitHub Actions if the repo host is ambiguous.

Done when: plan §M13 "Done when": a tagged commit produces a pullable image passing the smoke test.
