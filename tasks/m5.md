# Task M5 — Processor: single operations + snapshots

Read first: `tasks/_common.md`; `docs/plan.md` §M5 and §0; `docs/architecture-spec.md` §6 (validation), §7.2 (THE THREE GUARDS — read twice; they are the reason this system can be trusted), §8.1–8.2, §8.4 (snapshots), §11 (latency paragraph); `docs/decisions/0002-bidirectional-listen-notify.md` (governs the loop's idle wait); `CLAUDE.md` inviolables.

Prereq: M3 + M4 merged.

Mission: `internal/processor` main loop per plan §M5: config hot-reload, lease renew, PENDING fetch `ORDER BY id`, drain until empty select; idle wait = dedicated `LISTEN work_available` connection with deadline `min(loop_interval, time-to-safe-lease-renewal)` — doorbell wakes early, timed wakeup is the correctness path, notifications coalesced, ordering NEVER taken from notification arrival (G3), standbys don't listen. `process_single` in one DB tx with Guards 1–3 verbatim, rowcounts checked, any miss ⇒ rollback + re-acquire + continue. `internal/snapshot`: UTC-day cumulative upsert + sparse cascade (`day > :op_day`). Emit `NOTIFY outcomes, 'op:<id>'` inside the deciding commit (ADR-0002 outcome side; M8 consumes it).

Extend `simtest/` reference model: single-op validation + confirmed-balance evolution; assert G1 after every step.

Done when: plan §M5 "Done when" — including the doorbell wakeup test (idle leader, long loop_interval, decision lands well under one interval) and doorbell-loss test (suppressed notification; timed wakeup still decides within one interval). Verify: `make lint && make test && make itest`.
