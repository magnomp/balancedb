# Task M7 — HTTP API: insertion + queries (code-first OpenAPI via Huma)

Read first: `tasks/_common.md`; `docs/plan.md` §M7 and §0 (Huma stack decision); `docs/architecture-spec.md` §9 (read paths — implement the point-in-time and statement algorithms as specified), §10 (endpoints, status codes, machine-readable rejections), §6 (limit-change rules); `docs/decisions/0003-code-first-openapi-huma.md` (governs this task; note the boundary rule — Huma stays inside `internal/api`).

Prereq: M3 merged (M5/M6 useful for end-to-end tests but not required to start).

Mission: `internal/api` on Huma v2 over chi per plan §M7: every §10 endpoint as a typed operation; typed rejection-detail schemas; amounts as int64 fields with minor-units/JS-precision descriptions; `wait_ms` documented optional (0/omitted → immediate 202; sync waiting itself is M8 — stub `wait_ms > 0` to behave as 0 for now, documented as such); X-Owner-Id header param on every operation behind the pluggable owner-resolution interface; `/docs` + `/openapi.yaml` served by Huma; `make openapi` exports the committed `api/openapi.yaml`; CI diff check + `oasdiff` breaking-change check.

Done when: plan §M7 "Done when" — including the malformed-request (float amount) 7807 rejection test, a rendering + request-executing `/docs`, and a diff-clean `make openapi`. Verify: `make lint && make test && make itest && make openapi` (no diff).
