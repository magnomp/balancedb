# Task M1.5 — AI maintenance instructions (CLAUDE.md + docs)

Read first: `tasks/_common.md`; `docs/plan.md` §M1.5 (your full scope — follow its file list and CLAUDE.md section structure exactly) and §0; skim the whole of `docs/architecture-spec.md` (you are writing the file that points into it — the pointers must be right).

Prereq: M1 merged.

Mission: `CLAUDE.md` (≤150 lines, five sections per plan §M1.5: orientation, inviolables, code conventions, workflow, package map), `AGENTS.md` symlink, ADR skeleton (`docs/decisions/README.md` + template + `0001-custom-migration-runner.md` recording the plan §M2 decision; ADRs 0002/0003 already exist — integrate, do not renumber), `doc.go` per existing `internal/` package, lint check that CLAUDE.md and AGENTS.md stay in sync.

Constraint: do not paraphrase spec content into CLAUDE.md — point at sections. The inviolables list in plan §M1.5 is the required minimum, stated imperatively.

Done when: plan §M1.5 "Done when". The cold-session spot-check is your final self-test: re-read only CLAUDE.md and answer "what are the three guards and where do they live" and "how do I run integration tests" — if CLAUDE.md alone doesn't answer both, fix it before finishing.
