# Task M12 — Devcontainer

Read first: `tasks/_common.md`; `docs/plan.md` §M12 (config given in detail — follow it) and §0.

Prereq: M2 merged. NOTE: the plan recommends pulling this task forward to right after M2 — if you are running early in the sequence, that is intended; later tasks' agents benefit from it.

Mission: `.devcontainer/` per plan §M12: compose (dev service + postgres:18 with healthcheck + volume), devcontainer.json (ports, containerEnv incl. BALANCEDB_DATABASE_URL / TEST_DATABASE_URL, features: docker-in-docker + postgres-client, postCreateCommand, VS Code Go setup), Makefile targets completed as the maintenance interface (run-api, run-processor, test, itest, simtest, lint, image, loadgen, psql).

Done when: plan §M12 "Done when": fresh clone → Reopen in Container → `make itest` passes with zero manual setup; run-processor + run-api in two terminals = working local system.
